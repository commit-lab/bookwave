{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "prebuild": "npm install @rollup/rollup-linux-x64-gnu"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "NEXT_PUBLIC_VERCEL_ENV": "production"
  },
  "outputDirectory": "apps/web/.next"
}
