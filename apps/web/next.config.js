/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Should be uncommented when we add api-client.
  // transpilePackages: ["@bookwave/api-client"],
};

module.exports = nextConfig;
